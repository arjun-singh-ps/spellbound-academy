rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each family document is readable/writable only by the parent
    // account that owns it (the Firebase Auth uid IS the document id).
    match /families/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
